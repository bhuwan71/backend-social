export enum NotificationType {
  LIKE = 'like',
  COMMENT = 'comment',
  FRIEND_REQUEST = 'friend request',
}


export enum NotificationSettingType {
  EMAIL = 'email',
  PUSH = 'push',
  IN_APP = 'inApp',
}
